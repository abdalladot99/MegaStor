@inject UserManager<ApplicationUser> _userManager;
 @{
    Layout = null;
       
    string currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    
    string currentAction = ViewContext.RouteData.Values["action"]?.ToString();
         
    var user = await _userManager.GetUserAsync(User);
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Admin Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
 
    <!-- Bootstrap & Icons -->

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,500,700" rel="stylesheet">

    <link type="text/css" rel="stylesheet" href="~/css/bootstrap.css">
    <link type="text/css" rel="stylesheet" href="~/css/slick.css" />
    <link type="text/css" rel="stylesheet" href="~/css/slick-theme.css" />
    <link type="text/css" rel="stylesheet" href="~/css/nouislider.css" />
    <link type="text/css" rel="stylesheet" href="~/css/font-awesome.css" />
    <link type="text/css" rel="stylesheet" href="~/css/style.css" />
     
    <link rel="stylesheet" href="~/75945d6f89aa6662be59a903783c6a8e_logo.png">
     
    <link rel="stylesheet" href="~/lib/sweetalert2/sweetalert2.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MegaStor.styles.css" asp-append-version="true" />
    <link type="text/css" rel="stylesheet" href="~/css/StyleSheetAdmin.css">

</head>
<body>
    <!-- Premium Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <a  asp-controller="Home" asp-action="Index" class="brand">
                <div class="brand-logo">
                    <i class="fas fa-cube"></i>
                </div>
                <span class="brand-text">AdminPro</span>
            </a>
        </div>
        <div class="sidebar-menu">
            <!-- Dashboard -->
            <div class="menu-item-container @(currentController == "Admin" && currentAction == "Dashboard" ? "active" : "")">
                <a asp-controller="Admin" asp-action="Dashboard" class="menu-item">
                    <i class="fas fa-tachometer-alt menu-icon"></i>
                    <span class="menu-text">Dashboard</span>
                </a>
            </div>

            <!-- Users -->
            <div class="menu-item-container @(  currentAction == "Register" || currentAction == "GetAllUsers" || currentAction == "Newadmin" ? "active" : "")"
                 @if (currentAction == "Register" || currentAction == "GetAllUsers" || currentAction == "Newadmin")
                {
                     <text>data-expanded="true"</text>
                 }
>
        <div class="menu-item" onclick="toggleSubmenu(this.parentElement)">
            <i class="fas fa-users menu-icon"></i>
            <span class="menu-text">Users</span>
            <i class="menu-arrow fas fa-chevron-down"></i>
        </div>
        <div class="submenu">
            <a asp-controller="Admin" asp-action="GetAllUsers" class="submenu-item @(currentAction == "GetAllUsers" ? "active" : "")">
                <i class="fas fa-list"></i>
                <span>View All</span>
            </a>
            @if (User.IsInRole("Admin") && User.IsInRole("MainAdmin"))
            {
                <a asp-controller="AddAdmin" asp-action="Newadmin" class="submenu-item @(currentAction == "Newadmin" ? "active" : "")">
                      <i class="fas fa-user-plus"></i>
                      <span>Add New Admin</span>
                </a>
            }
            <a asp-controller="AccountSettings" asp-action="Register" class="submenu-item @(currentAction == "Register" ? "active" : "")">
                <i class="fas fa-user-plus"></i>
                <span>Add New User</span>
            </a>
        </div>
    </div>

            @if (User.IsInRole("Admin") && User.IsInRole("MainAdmin"))
            {
                <!-- Roles -->
                <div class="menu-item-container @(currentController == "RoleSettings" ? "active" : "")"
                @if (currentController == "RoleSettings")
                {
                    <text>data-expanded="true"</text>
                }
                >
                    <div class="menu-item" onclick="toggleSubmenu(this.parentElement)">
                        <i class="bi bi-shield-plus menu-icon"></i>
                        <span class="menu-text">Roles</span>
                        <i class="menu-arrow fas fa-chevron-down"></i>
                    </div>
                    <div class="submenu">
                        <a asp-controller="RoleSettings" asp-action="AddRole" class="submenu-item @(currentAction == "AddRole" ? "active" : "")">
                            <i class="fas fa-plus-circle"></i>
                            <span>Add Role</span>
                        </a>
                        <a asp-controller="RoleSettings" asp-action="ViewRoles" class="submenu-item @(currentAction == "ViewRoles" ? "active" : "")">
                            <i class="fas fa-list"></i>
                            <span>View Roles</span>
                        </a>
                        <a asp-controller="RoleSettings" asp-action="EditRole" class="submenu-item @(currentAction == "EditRole" ? "active" : "")">
                            <i class="fas fa-edit"></i>
                            <span>Edit Role</span>
                        </a>
                    </div>
                </div>
            }
            <!-- Categories -->
            <div class="menu-item-container @(currentController == "Category" ? "active" : "")"
                 @if (currentController == "Category")
                {
                     <text>data-expanded="true"</text>
                 }
>
        <div class="menu-item" onclick="toggleSubmenu(this.parentElement)">
            <i class="fas fa-boxes menu-icon"></i>
            <span class="menu-text">Categories</span>
            <i class="menu-arrow fas fa-chevron-down"></i>
        </div>
        <div class="submenu">
            <a asp-controller="Category" asp-action="Index" class="submenu-item @(currentAction == "Index" ? "active" : "")">
                <i class="fas fa-list"></i>
                <span>View All</span>
            </a>
            <a asp-controller="Category" asp-action="Create" class="submenu-item @(currentAction == "Create" ? "active" : "")">
                <i class="fas fa-plus"></i>
                <span>Add New</span>
            </a>
        </div>
    </div>

            <!-- Products -->
            <div class="menu-item-container @(currentController == "Product" ? "active" : "")"
                 @if (currentController == "Product")
                {
                     <text>data-expanded="true"</text>
                 }
>
        <div class="menu-item" onclick="toggleSubmenu(this.parentElement)">
            <i class="fas fa-box menu-icon"></i>
            <span class="menu-text">Products</span>
            <i class="menu-arrow fas fa-chevron-down"></i>
        </div>
        <div class="submenu">
            <a asp-controller="Product" asp-action="Index" class="submenu-item @(currentAction == "Index" ? "active" : "")">
                <i class="fas fa-list"></i>
                <span>View All</span>
            </a>
            <a asp-controller="Product" asp-action="Create" class="submenu-item @(currentAction == "Create" ? "active" : "")">
                <i class="fas fa-plus"></i>
                <span>Add New</span>
            </a>
        </div>
    </div>

            <!-- Orders -->
            <div class="menu-item-container @(currentController == "Order" ? "active" : "")"
                 @if (currentController == "Order")
                {
                     <text>data-expanded="true"</text>
                 }
>
        <div class="menu-item" onclick="toggleSubmenu(this.parentElement)">
            <i class="fas fa-shopping-cart menu-icon"></i>
            <span class="menu-text">Orders</span>
            <i class="menu-arrow fas fa-chevron-down"></i>
        </div>
        <div class="submenu">
            <a asp-controller="Order" asp-action="Index" class="submenu-item @(currentAction == "Index" ? "active" : "")">
                <i class="fas fa-list"></i>
                <span>View All</span>
            </a>
            <a asp-controller="Order" asp-action="Manage" class="submenu-item @(currentAction == "Manage" ? "active" : "")">
                <i class="fas fa-tasks"></i>
                <span>Manage</span>
            </a>
        </div>
    </div>

            <!-- Profile -->
            <div class="menu-item-container @(currentController == "AccountServices" ? "active" : "")">
                <a asp-controller="AccountServices" asp-action="MyAccount" class="menu-item">
                    <i class="bi bi-person-badge menu-icon"></i>
                    <span class="menu-text">My Profile</span>
                </a>
            </div>

            <!-- Logout -->
            <div class="menu-item-container">
                <a asp-controller="AccountSettings" asp-action="Logout" class="menu-item">
                    <i class="fas fa-sign-out-alt menu-icon"></i>
                    <span class="menu-text">Logout</span>
                </a>
            </div>
        </div>

 
  
     </div>

    <!-- Overlay (Mobile only) -->
    <div class="overlay" id="overlay"></div>

    <!-- Main content -->
    <div class="main-content" id="mainContent">
        <!-- Premium Header -->
        <div class="header">
            <button class="toggle-btn" id="toggleBtn">
                <i class="fas fa-bars"></i>
            </button>

            <div class="col-md-6">
                <div class="header-search" style="display: flex; align-items: center; gap: 0px;  width: 100%; ">
                      
                    <!-- فورم البحث -->
                    <form style="display: flex; align-items: center; flex: 1; gap: 1px;" method="get" asp-controller="Search" asp-action="Index">
                        <input name="query" class="input" placeholder="Search here"
                               style="flex: 1; min-width: 100px; padding: 6px 10px; font-size: 15px;" />
                        <button type="submit" class="search-btn"
                                style="padding: 6px 14px; white-space: nowrap;">
                            Search
                        </button>
                    </form>

                </div>
            </div>
             

            <div class="header-actions">
                <button class="action-btn">
                    <i class="fas fa-bell"></i>
                    <span class="notification-badge">3</span>
                </button>
                <button class="action-btn">
                    <i class="fas fa-envelope"></i>
                    <span class="notification-badge">5</span>
                </button>
                    <div class="user-profile" >
					<a asp-controller="AccountServices" asp-action="MyAccount" >
						<img src="~/img/@user.ImgUser" alt="User" class="user-avatar">
				    </a>
				    <div class="user-info">
                            <div class="user-name">@user.FullName</div>
                            <div class="user-role">Welcome ~ Administrator</div>
                        </div>
                    </div>
            </div>
        </div>

        <!-- Page Content -->
        <div class="page-content">
            <main role="main" class="pb-3">
                @RenderBody()
            </main>
        </div>
    </div>

 

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    @* <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script> *@


    <!-- jQuery Plugins -->

    <script src="~/js/jquery.js"></script>
    <script src="~/js/bootstrap.js"></script>
    <script src="~/js/slick.js"></script>
    <script src="~/js/nouislider.js"></script>
    <script src="~/js/jqueryz.js"></script>
    <script src="~/js/main.js"></script>

    <script src="~/js/Custom.js"></script>
    <script src="~/js/JavaScript.js"></script>


    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
     

    <script src="~/lib/sweetalert2/sweetalert2.min.js"></script>

    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        function toggleSubmenu(element) {
            // Close all other submenus first
            document.querySelectorAll('.menu-item-container').forEach(item => {
                if (item !== element) {
                    item.classList.remove('active');
                }
            });

            // Toggle current submenu
            element.classList.toggle('active');
        }

        // Close submenus when clicking outside
        document.addEventListener('click', function(e) {
            if (!e.target.closest('.menu-item-container')) {
                document.querySelectorAll('.menu-item-container').forEach(item => {
                    item.classList.remove('active');
                });
            }
        });
    </script>
    <script>
        const sidebar = document.getElementById("sidebar");
        const mainContent = document.getElementById("mainContent");
        const toggleBtn = document.getElementById("toggleBtn");
        const overlay = document.getElementById("overlay");

        let isMobile = window.innerWidth <= 992;

        const toggleSidebar = () => {
            if (isMobile) {
                sidebar.classList.toggle("show");
                overlay.classList.toggle("show");
            } else {
                sidebar.classList.toggle("collapsed");
                mainContent.classList.toggle("collapsed");
            }
        };

        toggleBtn.addEventListener('click', toggleSidebar);
        overlay.addEventListener('click', toggleSidebar);

        window.addEventListener('resize', () => {
            isMobile = window.innerWidth <= 992;
            if (!isMobile) {
                sidebar.classList.remove("show");
                overlay.classList.remove("show");
            }
        });
    </script>
    <script>
        // Initialize submenus based on current page
        document.addEventListener('DOMContentLoaded', function() {
            // Get all menu items with data-expanded="true"
            const expandedMenus = document.querySelectorAll('.menu-item-container[data-expanded="true"]');

            // Expand them
            expandedMenus.forEach(menu => {
                menu.classList.add('active');
            });
        });

        function toggleSubmenu(element) {
            // If clicking an already active menu, do nothing
            if (element.classList.contains('active')) return;

            // Close all other submenus first
            document.querySelectorAll('.menu-item-container').forEach(item => {
                if (item !== element) {
                    item.classList.remove('active');
                }
            });

            // Open the clicked submenu
            element.classList.add('active');
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>